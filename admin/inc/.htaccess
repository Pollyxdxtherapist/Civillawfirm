# The files here are included by PHP; none of them should ever be fetched
# directly. config.php in particular holds the database password.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
